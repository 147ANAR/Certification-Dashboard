---
description: Available npm scripts and how to run the project
globs: ["package.json"]
---

# Commands

- `npm run dev` — Start dev server with auto-reload (tsx watch), runs on port 3001
- `npm start` — Run server once (production)
- `npm run build` — Compile TypeScript to `dist/`
